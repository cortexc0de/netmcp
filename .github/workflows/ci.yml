name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_call:

env:
  PYTHONUNBUFFERED: "1"
  PYTHONDONTWRITEBYTECODE: "1"

jobs:
  lint:
    name: "Lint (ruff + mypy)"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Set up Python 3.12
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.12"
          cache: "pip"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Run ruff check
        run: ruff check src/netmcp/ tests/
      - name: Run ruff format check
        run: ruff format --check src/netmcp/ tests/
      - name: Run mypy
        run: mypy src/netmcp/ --ignore-missing-imports --no-error-summary
        continue-on-error: true

  test:
    name: "Test (Python ${{ matrix.python-version }})"
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Run tests with coverage
        run: |
          pytest tests/ -v --cov=netmcp --cov-report=xml --cov-report=term-missing \
            --cov-fail-under=40 --junitxml=pytest.xml
      - name: Upload coverage artifact
        if: matrix.python-version == '3.12'
        uses: actions/upload-artifact@ea165f8d65b6db9a8b9c8ce4e4b7845b535820e5 # v4.6.2
        with:
          name: coverage-report
          path: |
            coverage.xml
            pytest.xml
          retention-days: 30

  coverage-check:
    name: "Coverage gate (≥90%)"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [test]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Set up Python 3.12
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.12"
          cache: "pip"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Run tests with coverage threshold
        run: |
          pytest tests/ -v --cov=netmcp --cov-report=xml --cov-report=term-missing \
            --cov-fail-under=90
      - name: Coverage comment on PR
        if: github.event_name == 'pull_request'
        uses: MishaKav/pytest-coverage-comment@13d3c18e21895566c746187c9ea74736372e5e91 # v1.1.54
        with:
          pytest-xml-coverage-path: coverage.xml
          junitxml-path: pytest.xml
          title: "Coverage Report"

  build:
    name: "Build package"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Set up Python 3.12
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.12"
          cache: "pip"
      - name: Install build tools
        run: pip install build
      - name: Build wheel and sdist
        run: python -m build
      - name: Verify build artifacts
        run: |
          ls -la dist/
          test -f dist/*.whl || exit 1
          test -f dist/*.tar.gz || exit 1
      - name: Upload dist artifacts
        uses: actions/upload-artifact@ea165f8d65b6db9a8b9c8ce4e4b7845b535820e5 # v4.6.2
        with:
          name: dist
          path: dist/
          retention-days: 30

  security:
    name: "Security scan"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Set up Python 3.12
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.12"
          cache: "pip"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          pip install bandit safety
      - name: Run bandit (SAST)
        run: bandit -r src/netmcp/ -c pyproject.toml 2>/dev/null || bandit -r src/netmcp/ -ll
      - name: Check dependencies for vulnerabilities
        run: pip audit 2>/dev/null || safety check 2>/dev/null || echo "No vulnerability scanner available"

  publish:
    name: "Publish to PyPI"
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [lint, test, coverage-check, build, security]
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      id-token: write  # Trusted publishing
    steps:
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: dist
          path: dist/
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@76f52bc884231f62b54f72e44af12044e0e68f2a # v1.12.4
